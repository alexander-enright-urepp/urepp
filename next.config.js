/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'ovsfnjfznznfqkgtpuyy.supabase.co', 'img.youtube.com'],
    unoptimized: true,
  },
}

module.exports = nextConfig
